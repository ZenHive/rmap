---
sha: f88972e
subject: "phase 13 task 23 (bug): rmap depend mirrors target id storage shape for numeric-only string ids"
range: dcad8e5..HEAD
audit_run: 2026-05-17
auditor: claude-opus-4-7 + codex-companion
verdict: needs-fixes (applied, doc-only)
---

# audit(f88972e) — depend shape mirror

## Findings + dispositions

| # | Cat | Sev | Loc | Finding | Disposition |
|---|---|---|---|---|---|
| 1 | 6 | low | `src/mutate.rs:434` (`add_dependency_str` rustdoc) + `CLAUDE.md:86` | Shape-mirror semantics documented in private `target_id_shape` and CHANGELOG, but not in the public `add_dependency_str` rustdoc or CLAUDE.md mutation invariants — future mutator work could regress numeric-string handling | **applied to rustdoc only** — added a 3-sentence rustdoc paragraph on `add_dependency_str` explaining storage-shape mirroring. **Drop CLAUDE.md add** — adding a new invariant for one mutator's internal helper inflates the invariant index; the rustdoc + CHANGELOG are sufficient and the regression test in `tests/mutate.rs` is the load-bearing guard |

## Claude pass

- Cat 1-5: clean. The fix is well-contained: `target_id_shape` helper reads target storage shape; mutator mirrors. Both numeric-only-string and integer paths regression-tested in `tests/mutate.rs` and `tests/cli.rs depend_command`.
- Three-place edit N/A.

## Codex notes

Codex passed all Cat 1-5; only Cat 6 doc finding, partially applied.

## Verification

- `cargo test --test mutate`: 7/7
- `cargo test --test cli depend_command`: 8/8
- `cargo clippy --all-targets -- -D warnings`: clean

**Rating:** 9/10
**Severity ceiling:** nitpick — bug fix is clean, well-tested, and the surface that was missing rustdoc now has it.
