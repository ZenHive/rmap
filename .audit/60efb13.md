---
sha: 60efb13
subject: "phase 14 task 18: rmap import — paste-ready ROADMAP.md→tasks.toml migration prompt"
range: dcad8e5..HEAD
audit_run: 2026-05-17
auditor: claude-opus-4-7 + codex-companion
verdict: needs-fixes (applied)
---

# audit(60efb13) — rmap import

## Findings + dispositions

| # | Cat | Sev | Loc | Finding | Disposition |
|---|---|---|---|---|---|
| 1 | 1 | serious | `templates/import_prompt.md:24`; `src/render.rs:455,457` | Status glyph mapping reversed: template says `⛔ → blocked` and `🔁 → superseded`, but `rmap render` emits `🔶` for blocked and `⛔` for superseded. Following the prompt corrupts statuses on round-trip. | **applied** — swapped to match render output; also clarified "(as emitted by `rmap render`)" |
| 2 | 1 | moderate | `templates/import_prompt.md:60`; `src/schema.rs:70` | Prompt told agents to omit `id` when source lacks it; `Task.id` is required and verification step 1 (`rmap validate`) would reject the file. | **applied** — rewrote the bullet to explicitly state `Task.id` is required, give two clear options (assign sequential ids, or use `rmap new --from-stdin` afterward for auto-allocation), and warn that omitting `id` in `tasks.toml` will be rejected |
| 3 | 1 | moderate | `src/main.rs:165` | CLI help: "does not read or write any files" — but `Commands::Import` calls `validate_tasks_file(...)` to populate the project name. | **applied** — rewrote help to "Prints to stdout; never writes. Reads existing tasks.toml if present (only to populate the Project: line) — falls back to a placeholder when none is found." |
| 4 | 1 | minor | `src/import.rs:7` | Template interpolation is chained raw `.replace(...)` over unrestricted `project: String` — a project name containing `{{schema_json}}` would expand during the second replace, and embedded newlines could shift Markdown structure | **drop** — project name is read from the user's own validated `tasks.toml` (single-tenant, trusted source). Defense-in-depth would justify, but this is below the auto-apply tier for a known-trusted input and any switch to a real templating engine is convergent with finding #5 |
| 5 | 4 | nitpick | `src/import.rs:7` vs `src/render_html.rs:113` | One-off `.replace()` template renderer despite the repo already using `minijinja` via `roadmap.html.j2` | **drop** — nitpick; two consumers don't justify a shared abstraction yet; the day a third template lands, extract |

## Claude pass

Checked for additional Cat 1-6:

- Acceptance criteria for Task 18 (per ROADMAP) are satisfied by the commit; `SKILLS.md` updated.
- No actionable TODOs left in `src/`.
- Three-place edit N/A (no new schema field).

## Codex notes

- All Codex Cat 1 findings verified against current code — the glyph reversal was the worst (silent status corruption on migration).

## Verification

- `cargo test`: green
- `cargo run -- import` output inspected: glyph row now matches `render.rs` emit
- `cargo clippy --all-targets -- -D warnings`: clean

**Rating:** 6/10
**Severity ceiling:** serious — the glyph reversal would have silently corrupted blocked/superseded tasks on every migration. Patched, but the failure mode (template hand-maintained against an enum that's centralized elsewhere) is the kind that recurs.
