---
sha: 51532ba
subject: "add milestones rmap tasks"
range: dcad8e5..HEAD
audit_run: 2026-05-17
auditor: claude-opus-4-7 + codex-companion
verdict: needs-fixes (applied)
---

# audit(51532ba) — milestones tasks added

## Findings + dispositions

| # | Cat | Sev | Loc | Finding | Disposition |
|---|---|---|---|---|---|
| 1 | 1 | moderate | `roadmap/tasks.toml:25`, `ROADMAP.md:89` | Task 24 added as `pending` in phase 15, but `[phases.15]` and rendered ROADMAP heading still mark Phase 15 as `done` — schema accepts the incoherence but the roadmap-as-queue contract breaks | **applied** — flipped `[phases.15].status` from `"done"` to `"in_progress"`; re-rendered; phase header now reflects "Schema extensions (in_progress)" |

## Claude pass

- Task 24 itself is well-formed (id, phase, bundle, milestone metadata schema spec, scores, body, ACs)
- New bundle `schema_milestones` correctly declared
- Three-file atomicity satisfied

## Codex notes

- `cargo run` blocked in sandbox; binary-fallback `./target/debug/rmap validate --check-render` returned `valid` — confirmed locally post-fix.

## Verification

- `cargo test`: green
- `rmap validate --check-render`: valid (after re-render)
- `rmap list --status pending` shows task 24 alongside task 9

**Rating:** 7/10
**Severity ceiling:** moderate — schema permitted the incoherence (pending task inside done phase), but the rendered output gave the wrong queue signal. Now corrected.
