---
sha: 53f0023
subject: "docs(CLAUDE.md): trim to invariant index — 51KB → 13KB"
range: dcad8e5..HEAD
audit_run: 2026-05-17
auditor: claude-opus-4-7 + codex-companion
verdict: needs-fixes (applied as part of 794036e's CLAUDE.md batch)
---

# audit(53f0023) — CLAUDE.md trim

## Findings + dispositions

| # | Cat | Sev | Loc | Finding | Disposition |
|---|---|---|---|---|---|
| 1 | 6 | minor | `CLAUDE.md:75-76` | Marker-boundary invariant only enumerates TASKS/FOCUS/MERMAID; VISION marker pair is implemented in `render.rs` (`VISION_BEGIN_MARKER` / `VISION_END_MARKER`) and exercised by the `vision_block` golden fixtures — but absent from the trimmed index | **applied** (in 794036e's CLAUDE.md edit batch) — added VISION to both marker invariant lines |
| 2 | 6 | nitpick | `CLAUDE.md:49-95` (pre-trim) → trimmed | Detailed TASKS row shape removed; behavior remains locked by render code/golden tests | **drop** — golden tests are load-bearing; CLAUDE.md needn't duplicate them |
| 3 | 6 | nitpick | `CLAUDE.md` pre-trim → trimmed | Some validation strings (`unsupported schema_version`, `blocked but missing blocked_reason`) no longer indexed in CLAUDE.md, but backed by source/tests | **drop** — `must be in 1..=10` retained as the load-bearing one; the others are findable by grep in tests/ |
| 4 | 6 | nitpick | `CLAUDE.md` pre-trim → trimmed | `DoctorFinding.kind` enum list removed | **drop** — `doctor --json` additive-only invariant retained; enum names discoverable via source/tests |

## Claude pass

The trim preserved every load-bearing invariant the project relies on (D/B/U range, marker boundaries, three-place edit rules, additive `--json`, `today_iso()` source, mutator validate-then-write, etc.). One real gap: VISION markers were dropped. Now fixed.

## Codex notes

Codex's pass was thorough — categorized findings as "preserved / moved / dropped". Only the dropped item warranted action.

## Verification

- `cargo test`: green
- VISION marker present in CLAUDE.md after edit

**Rating:** 8/10
**Severity ceiling:** minor — VISION omission was real but caught and patched; the broader trim preserved the right substance.
