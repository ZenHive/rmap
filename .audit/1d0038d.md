---
sha: 1d0038d
subject: "phase 13 task 20: rmap status auto-fills started_at / done_at on transition"
range: dcad8e5..HEAD
audit_run: 2026-05-17
auditor: claude-opus-4-7 + codex-companion
verdict: needs-fixes (applied)
---

# audit(1d0038d) — status auto-fills timestamps

## Findings + dispositions

| # | Cat | Pri | Loc | Finding | Disposition |
|---|---|---:|---|---|---|
| 1 | 1 | 6 | `src/mutate.rs:169` (current `update_status_many_str`) | `today_iso()` called per matched task inside the loop — a bulk transition `rmap status 1,2,3 done` that straddles midnight stamps the first task with day N and later tasks with day N+1, breaking the bulk-atomicity-feel of timestamps | **applied** — snapshot `today_iso()` once before the loop into a `let today` bound by `timestamp_field`'s presence; reused via `.as_deref()` |

## Claude pass

No additional findings. Three-place edit rule N/A (timestamp insertion, not a new schema field). Tests cover done/in_progress/preservation/bulk paths.

## Codex notes

- `cargo clippy --all-targets -- -D warnings` blocked in sandbox; ran locally post-fix — clean.
- Tests focused around `tests/mutate.rs` and `tests/cli.rs` (status command) pass.

## Verification

- `cargo build`: clean
- `cargo test --test mutate`: pass
- `cargo test --test cli`: pass
- `cargo clippy --all-targets -- -D warnings`: clean

**Rating:** 8/10
**Severity ceiling:** moderate — pointed-out midnight edge was a real consistency gap; cheap fix; one-clean Codex finding with no false positives.
