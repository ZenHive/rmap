# Post-merge audit: 198ec0b

Reviewed landed commits `040cb8e..198ec0b`, covering task-routing metadata, graph queries and exports, `rmap new` batch validation, doctor advisories, CI maintenance, and their documentation.

## Findings and fixes

1. The CI workflow introduced trailing whitespace. Removed it from `.github/workflows/main.yaml`.
2. The shipped changes for Tasks 43, 47, 48, 50, and 51 had no corresponding `CHANGELOG.md` entries. Added a concise Unreleased summary covering the domains field, doctor advisories, graph export/waves, and atomic stdin validation.

No dead code, debug output, stale user-facing command documentation, or reviewer-rejection follow-up was found. No discovery tasks were filed.

## Verification

Cold-tree check passed:

```sh
cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test
```
