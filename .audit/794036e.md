---
sha: 794036e
subject: "phase 15 task 22: Task::implemented field — record what was delivered (schema_version 2)"
range: dcad8e5..HEAD
audit_run: 2026-05-17
auditor: claude-opus-4-7 + codex-companion
verdict: needs-fixes (applied)
---

# audit(794036e) — Task::implemented, schema_version 2

## Findings + dispositions

| # | Cat | Pri | Loc | Finding | Disposition |
|---|---|---:|---|---|---|
| 1 | 1 | 7 | `src/validate.rs:434` | `validate_implemented` accepted `implemented = ""` for done tasks — empty string passed the `is_some()` check, defeating the conditional-required invariant | **applied** — switched to `as_deref().is_some_and(|s| !s.is_empty())`; mirrors blocked_reason posture |
| 2 | 1 | 4 | `src/main.rs:1022` | `rmap status 1,99 done` (TTY) prompts for `implemented` on task 1 before discovering task 99 is unknown — UX papercut, not a correctness gap | **drop** — Pri 4 < auto-apply tier; prompt aborts cleanly with ctrl-C; restructuring to pre-validate ids would split `update_status_many_str`'s atomicity guard between two phases |
| 3 | 6 | 6 | `README.md:35,94` | Quickstart `tasks.toml` uses `schema_version = 1` — file fails validation against current v2 schema | **applied** — bumped both occurrences to `schema_version = 2` |
| 4 | 6 | 5 | `README.md:63` | Quickstart `rmap status 1 done` lacks `--implemented "..."` — fails validation post-Task-22 | **applied** — added `--implemented "wired up postgres + ecto repo"` |
| 5 | 6 | 6 | `DESIGN.md:222,230` | Design prescribes `rmap migrate --from 1 --to 2` for schema upgrades but no such command exists (and per DESIGN's own "Out of scope" list, none is planned) | **applied** — rewrote both lines to reflect LLM-driven upgrades (per DESIGN's stated posture) |
| 6 | 6 | 5 | `CLAUDE.md:80` | Load-bearing invariant still pinned to `schema_version = 1` | **applied** — bumped to v2; also added the new `implemented`-required invariant alongside `blocked_reason` |
| 7 | 6 | discuss-design | `src/schema_json.rs:4` | JSON Schema doesn't encode the done→implemented conditional-required invariant | **drop** — JSON Schema's `if/then` for conditional-required across enum tags is awkward to express via `schemars` derives without hand-edits; documented in CLAUDE.md instead; same pattern as blocked_reason which isn't in the JSON schema either |

## Bonus (Claude pass)

Added VISION marker to CLAUDE.md's marker-boundary invariant (was in 53f0023 report but landed here as a single CLAUDE.md edit batch).

## Codex notes

- `cargo clippy --all-targets -- -D warnings` blocked in Codex sandbox (target/debug/.cargo-lock); ran locally post-merge — clean.
- All 21 done tasks in `roadmap/tasks.toml` carry `implemented`; verified at HEAD.

## Verification

- `cargo test`: 27 + 48 + 119 + 14 + 12 + 4 + 9 + 7 + 11 + 4 + 3 + 5 + 2 + 1 = all green
- `cargo clippy --all-targets -- -D warnings`: clean
- `cargo fmt --check`: clean
- `rmap validate --check-render`: valid

**Rating:** 7/10
**Severity ceiling:** serious — pre-fix `validate_implemented` accepted empty string, defeating the new invariant on day one. Caught and patched.
